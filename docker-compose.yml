services:
  pm4py:
    image: pm4py-llm
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pm4py-llm-container
    volumes:
      - .:/pm4py-llm
    ports:
      - "8000:8000"
